# Email Configuration
# Copy this file to .env and fill in your actual values

# ============================================================
# Email Provider Selection
# ============================================================
# Choose 'sendgrid' or 'smtp'
EMAIL_PROVIDER=sendgrid

# ============================================================
# SendGrid Configuration (when EMAIL_PROVIDER=sendgrid)
# ============================================================
SENDGRID_API_KEY=your-sendgrid-api-key-here
FROM_EMAIL=auxct@example.com

# ============================================================
# SMTP Configuration (when EMAIL_PROVIDER=smtp)
# ============================================================
# SMTP server hostname
# Examples:
#   Gmail: smtp.gmail.com
#   Outlook: smtp-mail.outlook.com
#   Yahoo: smtp.mail.yahoo.com
SMTP_HOST=smtp.example.com

# SMTP port
# Common ports:
#   587 - TLS (recommended)
#   465 - SSL (legacy)
#   25  - Unencrypted (not recommended)
SMTP_PORT=587

# Use TLS encryption (true/false)
# Set to 'true' for port 587, 'false' for port 465 or 25
SMTP_USE_TLS=true

# SMTP username (usually your full email address)
SMTP_USER=your-email@example.com

# SMTP password
# For Gmail: Use an app-specific password (not your regular password)
#   Generate at: https://myaccount.google.com/apppasswords
# For Outlook/Yahoo: May need app-specific password or regular password
SMTP_PASSWORD=your-password-here

# From email address (optional, defaults to SMTP_USER if not set)
# FROM_EMAIL=auxct@example.com
